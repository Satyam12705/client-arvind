# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cloudflare Workers/Pages local dev state and secrets — never commit these.
.wrangler
.dev.vars

# Source purchase orders / service orders / work orders used to compile project
# data — contain client contract terms, contacts and pricing; not for the repo.
myprojects/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
